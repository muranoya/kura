[package]
name = "vault_jni"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
vault_core = { path = "../../vault-core", features = ["android"] }
jni = "0.21"
serde_json = "1"
